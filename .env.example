# ─── Pagamento PIX ─────────────────────────────────────────────────
# Credenciais do provedor de pagamentos (ci / cs headers HTTP)
MISTICPAY_CLIENT_ID=seu_client_id
MISTICPAY_CLIENT_SECRET=seu_client_secret
MISTICPAY_WEBHOOK_URL=https://gta6.xdeals.online/webhook

# Observação: o projeto NÃO usa banco de dados. Os pedidos ficam em memória
# (ver src/lib/store.ts). Para persistência total em produção serverless,
# considere integrar Vercel KV no futuro.
